{
  "name": "cryptic-hub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Cryptic Hub — curated directory, ARIA assistant and member messages, with Firebase email-verified auth.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.17.1"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
